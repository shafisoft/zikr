# loop workspace

This directory was scaffolded by `loop init`.

- `.loop/pipelines/` — deterministic stage workflows. Run with `loop run <file>`.
- `.loop/rooms/` — multi-agent chat rooms. Open with `loop chat <file>`.

Start here:

    loop validate .loop/pipelines/feature-delivery.yaml
    loop validate .loop/rooms/leadership.yaml
