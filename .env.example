# Supabase project for the Shared Goals (rooms) feature.
# Copy this file to `.env` and fill in your project values.
# See supabase/README.md and docs/SharedGoals-Design.md
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Cloudflare Turnstile site key — enables CAPTCHA on anonymous sign-in.
# Create a widget at https://dash.cloudflare.com/?to=/:account/turnstile,
# paste the SECRET key into Supabase (Auth → Settings → CAPTCHA protection),
# and the SITE key here. Leave empty to disable captcha entirely.
#
# Widget settings that matter:
#   - Widget Mode: "Managed" (Invisible has no interactive fallback and
#     fails real users when passive verification can't pass).
#   - Hostnames: add the production host AND localhost for local dev
#     (per https://supabase.com/docs/guides/auth/auth-captcha).
#   - For pure local work you can also use Cloudflare's always-pass test
#     sitekey "1x00000000000000000000AA" (its matching test secret goes
#     into a DEV Supabase project — never the production one).
VITE_TURNSTILE_SITE_KEY=

# Optional: run the Group tab against the dumb in-memory backend instead of
# Supabase (no network, data resets on reload). Values: mock | supabase
VITE_SHARED_ROOMS_BACKEND=supabase
